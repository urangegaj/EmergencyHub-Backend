apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: emergency-hub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        args: ["redis-server", "--maxmemory-policy", "allkeys-lru"]
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: emergency-hub
spec:
  selector:
    app: redis
  ports:
  - port: 6379
    targetPort: 6379
